{
  "name": "iptv-extractor",
  "version": "1.0.0",
  "scripts": {
    "extract": "node extractor.js",
    "update-github": "npm run extract && git add . && git commit -m \"Actualización automática\" && git push"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.43.1"
  }
}
